// Selector function to get the project parameters from the Redux store
export const selectProjectParameters = state => state.projectParameters;

// Selector function to get the images from the Redux store
export const selectImages = state => state.imageData;